services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  gateway:
    build: 
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
      - APP_ENV=development
    depends_on:
      - redis